function createCustomizeClassString(styles){

    const style = ``

    if(styles.dualtone){
        styles += `bg-${styles.color}-100 text-${styles.color}-500`
    }
    else if(styles.outline){

    }
    else if(styles.bodyless)

    
    return style
}